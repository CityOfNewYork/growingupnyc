{% extends "base.twig" %}

{% block content %}
<h2>Upcoming Events</h2>
{% for post in events %}
  {% include ['partials/teaser-'~post.post_type~'.twig', 'partials/teaser.twig'] %}
{% endfor %}
<a href="/events">See Full Calendar</a>

{# Age/Milestone Picker #}
<ol class="js-accordion c-list-box c-list-box--primary" data-multiselectable="true">
  {% for age_group in age_menu %}
    <li class="c-list-box__item {{age_group|sanitize}}-background">
      <h3 class="js-accordion__header c-list-box__heading" id="homepage-ages-heading-{{loop.index}}">
        {{age_group.name}}
        <span class="c-list-box__subheading">{{age_group.description}}</span>
      </h3>
      <ol class="js-accordion__content c-list-box__content" id="homepage-ages-panel-{{loop.index}}">
        {% for post in age_group.posts('numberposts=-1&post_type=age&meta_key=numeric_age&orderby=meta_value_num&order=ASC') %}
          <li><a href="{{post.link}}" class="c-list-box__subitem">{{post.title}}</a></li>
        {% endfor %}
      </ol>
    </li>
  {% endfor %}
</ol>
{% endblock %}