<svg role="img" class="icon--xxsmall"><use xlink:href="#map-marker"></use></svg><a href="{{post.google_map_link}}" target="_blank">{{post.venue}},
  {% for borough in post.terms('borough') %}
    {{borough.name}}
  {% endfor %}
</a>
<div class="salog"> Source file : event-link.twig</div>
